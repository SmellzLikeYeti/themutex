type User {
  username: String!
  password: String
  email: String
}

type Profile {
  fName: String
  lName: String
}

type Query {
  user(username: String!): User
}

type Mutation {
  createUser(username: String!, password: String!, email: String!): User
}
